# 2026-09-17
- 修复 server/app.py 的 1045 密码报错排查：真因是环境变量 MYSQL_PASSWORD 未设置/不匹配；MySQL root 真实密码存于 run_scrape.bat（zc123456），验证连接成功（pin 表 242 条）。
- app.py 加固：SCRAPER_PATH/DATA_DIR 从写死的 D:/11/juejin 改为基于 __file__ 的自动相对路径（PROJ_DIR 上级目录）。
- 新增：pymysql.OperationalError 全局 errorhandler（1045/2003/1049 翻译成人话 JSON）、启动时 MySQL 预检、抓取线程 try/finally 防 running 卡死、抓取脚本缺失提示、DATA_DIR 自动创建。
- 用 Flask test_client 验证 /api/stats、/api/pins 均 200。
- 全项目旧路径清理（D:/11/juejin → D:/bao/juejin）：juejin_pins.py / notify.py / juejin_checkin.py 的 DATA_DIR、FLAG_DIR 改为 os.path.dirname(os.path.abspath(__file__)) 自动定位；run_scrape.bat、README.md 同步更新。
- 注意：login_save.py 仍在 D:/11/scripts/（未搬家），各脚本提示语里的该路径保留。
- 验证：三脚本语法通过，DATA_DIR/FLAG_DIR/AUTH_PATH 解析均指向 D:/bao/juejin/data，auth_juejin.json 存在。
- 修复"密码对却 1045"问题：真凶是 CMD 语法 `set MYSQL_PASSWORD=zc123456 && python ...` 会把 && 前的空格并入变量（实际值为 "zc123456 "）。app.py 的 get_conn() 与 juejin_pins.py 的 load_mysql_conf() 均加 .strip() 兜底。
- 正确写法：`set "MYSQL_PASSWORD=zc123456"` 后另起一行运行，或直接 `set MYSQL_PASSWORD=zc123456&&python ...`（&& 紧贴）。
- 修复网页抓取日志乱码：子进程管道输出默认 GBK、父进程按 UTF-8 解码所致。app.py 的 scrape() 里给子进程 env 注入 PYTHONIOENCODING=utf-8 对齐编码。端到端验证（2条/0评论）日志全部中文正常。
- 二次乱码排查：修复代码有效，但用户运行的 Flask 是修复前启动的旧进程（debug=False 不热重载）。已加启动横幅 build 标记 + use_reloader=True，以后改代码自动生效。
- 修复 CMD 直跑 juejin_pins.py 时 emoji 导致的 UnicodeEncodeError（GBK 控制台 print 📷 崩溃）：脚本头部统一 sys.stdout/stderr.reconfigure(encoding=utf-8, errors=replace)。实测 GBK 环境下无保护崩溃、加保护后正常输出。
- 向用户说明了抓取原理：requests 直接 POST 掘金网页版内部 API（api.juejin.cn），非浏览器自动化；登录态来自 data/auth_juejin.json 的 cookie 注入。
- 按用户要求放慢抓取节奏: WORKERS 5->2, PAGE_DELAY 0.3->1.0s, PIN_DELAY 0.5->1.5s（防限流/风控）。
- 大重构（应用户 5 项需求）：
  1) 新增 jjconfig.py 统一配置（config.json 存 MySQL 密码，环境变量可覆盖，模板 config.example.json），requirements.txt、.gitignore（排除 config.json 与 data/）。
  2) login_save.py 内迁到项目根：playwright 有头 + 轮询自动检测登录成功保存，无需回车，消除 D:/11/scripts 外部依赖。
  3) server/app.py 重写：接入 jjconfig（不再强制环境变量）；新增签到 API（/api/checkin）、登录态 API（/api/auth/status、/api/auth/login）、网页定时抓取 API（/api/schedule，持久化到 config.json，重启恢复）；run_script_job 通用后台任务框架；use_reloader=False。
  4) index.html 新增：签到面板（按钮+日志+最近结果）、登录态标签+一键登录、自动抓取开关（间隔设置+下次运行时间）。
  5) 脚本族全部相对路径：auto_scrape.py（读 config.json）、run_scrape.bat（%~dp0，去掉密码）、环境安装.bat、一键启动看板.bat、安装/卸载定时任务.bat。
  6) 旧 README 存档 docs/README_旧版.md，新 README 为部署向（本机 3 步 / 异机 4 步 / FAQ / 故障排查）。
- 端到端验证全过：7 个 py 语法 OK；无环境变量纯 config.json 驱动下全部 API 200；定时器开(1800s)/关持久化 OK；全项目写死路径扫描为 0。
- 新增 docs/手动部署流程.md：命令行版 7 步部署（环境确认→依赖→建库→配置→登录态→首抓建表→看板→可选计划任务）+ 手动版专属 FAQ；README 目录树和异机部署节已加链接。
- 签到/抽奖拆分（应用户需求）：juejin_checkin.py 增加 --mode all/checkin/lottery；抽奖改 API 方案（lottery_config/get 查免费次数 + lottery/draw 抽 + get_cur_point 积分），只在免费次数>0 时抽、绝不花积分，返回抽中奖品与剩余矿石（points/points_before 都入日志）；页面文本兜底逻辑保留。
- /api/checkin 支持 mode 参数；index.html 签到面板拆三个按钮（仅签到/免费抽奖/签到+抽奖）+ 不耗积分提示；最近结果里 points 为 null 时不显示。
- 真实实测 mode=lottery：当前矿石 2106，今日免费已用完→正确跳过不抽，日志与 checkin_log.json 记录正常。
- 前端时间控制（应用户需求）：① 自动抓取间隔由手输秒数改为按钮组（30分/1时/2时/6时/12时），旧配置非标值回落到 1800；② 沸点列表加发布时间范围筛选（全部/今天/近3/近7/近30天），后端 /api/pins 增加 days 参数（DATE_SUB CURDATE，近n天=n-1），与关键词可组合，非法值兜底为全部。实测各档位与组合筛选正常。
- ① 自动抓取间隔加"自定义"档（分钟输入 5-1440，内部转秒，持久化回显到自定义档）；② 抽奖加矿石补抽：juejin_checkin.py --use-points/--max-paid（免费用完后按 200 矿石/次补抽，矿石<200 或接口拒绝自动停），/api/checkin 透传 use_points/max_paid，前端开关+次数输入，设置存 localStorage。默认关闭绝不花积分。
- 抓取节奏三参数（WORKERS/PAGE_DELAY/PIN_DELAY）前端化：juejin_pins.py 启动时从 config.json 的 scrape 节覆盖默认值（范围校验 1-10/0-30/0-30）；app.py 新增 /api/scrape/speed GET/POST（含 reset）；index.html 抓取控制加"高级设置"折叠面板（三输入框+默认值提示+保存/恢复默认按钮）。端到端验证：默认读、保存、持久化、越界拒绝、恢复默认、脚本实际读取覆盖值全通过。
- 文档同步补齐：README 功能表新增（时间筛选/高级设置/三按钮签到/矿石补抽/自定义间隔），FAQ 三条改写（限流调参入口、矿石说明、签到抽奖分离命令）；手动部署文档新增第 6.5 步（签到/抽奖命令行用法含 --use-points）、FAQ 加两条（免费次数保护、限流入口）。
- 应用户要求抓取节奏默认值改回快速档：WORKERS 2->5, PAGE_DELAY 1.0->0.3, PIN_DELAY 1.5->0.5，五处同步（juejin_pins.py / jjconfig.py 模板 / app.py SPEED_DEFAULTS / config.json 当前值 / index.html 提示与初值），README 同步。验证脚本生效值与 API 返回一致。
